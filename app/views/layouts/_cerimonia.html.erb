<div id="Boxcerimonia">
	<ul>
    	<li>Data: <%=@info.data_cerimonia.to_date.strftime("%d/%m/%Y")%></li>
	    <li>Horário: <%=@info.data_cerimonia.to_time.strftime("%H:%M:%S")%></li>
	    <li>Local: <%=@info.local_cerimonia%></li>
		<%if current_user%>
			<%=link_to image_tag("icons/editar.png") + "editar", edit_info_path(@info)%>
		<%end%>
    </ul>
</div>