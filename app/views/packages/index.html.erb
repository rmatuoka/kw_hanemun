<%content_for :head do%>
	<%=stylesheet_link_tag "lista_hoteis"%>
<%end%>

<div id="Boxcenter">
           <%=render :file => "/layouts/_top.html.erb"%>
           <div id="Vazia"></div>
            <div id="Bottomboxc">
            	<h1 class="Titlista">
				MEUS PACOTES 
				<%=link_to "+ adicionar novo", new_package_path%></h1>
                <%@packages.each do |p|%>
				<div id="Boxlista">
                    <p>
						<%if current_user%>
							<%=link_to "Excluir", p, :confirm => 'Confirme a exclusão do pacote.', :method => :delete%> | 
						<%end%>
						<b><%=p.nome%></b>
						
						<%if current_user%>
							<%= link_to "Editar", edit_package_path(p) %>
							<%= link_to "Gerenciar Cotas", package_quotes_path(p) %>
						<%end%> 
					</p>
                </div>
				<%end%>
            </div>
        </div>
